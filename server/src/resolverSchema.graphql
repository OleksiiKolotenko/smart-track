type User {
  id: ID!
  name: String!
  role: String!
  email: String!
  phone: Float!
  rooms: [String]
}

type Alert {
  number: ID!
  status: String!
  color: String!
}

type Room {
  id: ID!
  name: String!
  ownerId: String
  ownerName: String
}

type Query {
  getUsers: [User]
  getByRole(role: String!): [User]
  getAlerts: [Alert]
  getRooms: [Room]
  getRoomsById(ownerId: String): [Room]
}
